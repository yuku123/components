<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zifang.z.config.core.domain.mapper.ZPermissionsMapper">

    <!-- 基础结果集映射 -->
    <resultMap id="BaseResultMap" type="com.zifang.z.config.core.domain.entity.ZPermissions">
                <id column="role" property="role" />
                <id column="resource" property="resource" />
                <id column="action" property="action" />
    </resultMap>

    <!-- 基础查询字段 -->
    <sql id="Base_Column_List">
            role,
            resource,
            action
    </sql>

    <!-- 新增语句 -->
    <insert id="insert" parameterType="com.zifang.z.config.core.domain.entity.ZPermissions">
        INSERT INTO z_permissions (
        ) VALUES (
        )
    </insert>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM z_permissions
        WHERE
                role = role
                resource = resource
                action = action
    </select>

    <!-- 更新语句 -->
    <update id="updateById" parameterType="com.zifang.z.config.core.domain.entity.ZPermissions">
        UPDATE z_permissions
        SET
        WHERE
                role = role
                resource = resource
                action = action
    </update>

    <!-- 删除语句 -->
    <delete id="deleteById">
        DELETE FROM z_permissions
        WHERE
                role = role
                resource = resource
                action = action
    </delete>

</mapper>